services:
  pomaidb-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: pomaidb/dev:local
    container_name: pomaidb_dev
    working_dir: /workspace/pomaidb
    command: ctest --test-dir build --output-on-failure
    volumes:
      - ./:/workspace/pomaidb
