/*
 * src/ai/candidate_collector.cc
 *
 * Implementation file for CandidateCollector.
 *
 * (This file intentionally left minimal: the header contains the full
 * implementation in simple C++ for readability. This translation unit
 * exists to allow future non-inline helpers and keep header light.)
 */

#include "src/ai/candidate_collector.h"

namespace pomai::ai
{
    // All implementation lives in the header for simplicity.
    // The .cc file is provided to satisfy build systems that prefer .cc TUs.
} // namespace pomai::ai